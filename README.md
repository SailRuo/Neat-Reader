# Neat Reader - 简洁优雅的电子书阅读器

> 基于 Electron + Vue 3 的跨平台桌面电子书阅读应用

## 📖 简介

Neat Reader 是一个功能完整的电子书阅读器，支持 EPUB 和 PDF 格式，集成百度网盘云同步功能。

**本项目已从 Wails (Go) 迁移到 Electron (Node.js)，主要目的是解决 EPUB iframe 渲染问题。**

## ✨ 核心功能

- 📚 **多格式支持** - EPUB、PDF 电子书阅读
- ☁️ **百度网盘集成** - 云端存储和同步
- 💾 **本地存储** - 离线阅读，数据持久化
- 📊 **阅读进度** - 自动保存，跨设备同步
- 🏷️ **分类管理** - 自定义书籍分类
- 🎨 **阅读设置** - 字体、主题、亮度调节

## 🚀 快速开始

### 前置要求

- Node.js 16+ ([下载地址](https://nodejs.org/))
- npm 或 yarn

### 方式 1：使用安装脚本（推荐，解决网络问题）

**Windows 用户：**
```cmd
install.bat
```

**macOS/Linux 用户：**
```bash
chmod +x install.sh
./install.sh
```

安装完成后启动：
```cmd
start.bat        # Windows
./start.sh       # macOS/Linux
```

### 方式 2：手动安装

```bash
# 1. 安装所有依赖（首次运行）
npm install

# 2. 启动开发模式
npm run dev
```

> **注意**：如果 `npm install` 失败（特别是 Electron 下载失败），请使用方式 1 的安装脚本，它会自动配置国内镜像源。

启动后会自动打开应用窗口，同时运行：
- 后端服务器：http://localhost:3000
- 前端开发服务器：http://localhost:5173

## 📁 项目结构

```
neat-reader/
│
├── electron/                    # Electron 主进程
│   ├── main.js                 # 应用入口、窗口管理
│   ├── preload.js              # 安全的前后端通信桥梁
│   └── menu.js                 # 应用菜单配置
│
├── backend/                     # Node.js 后端服务
│   ├── src/
│   │   ├── server.js           # Express HTTP 服务器
│   │   ├── routes/             # API 路由
│   │   │   └── baidu.js        # 百度网盘 API 路由
│   │   ├── services/           # 业务逻辑层
│   │   │   └── baiduService.js # 百度网盘服务实现
│   │   └── utils/              # 工具函数
│   │       └── logger.js       # 日志工具
│   └── package.json            # 后端依赖
│
├── frontend/                    # Vue 3 前端应用
│   ├── src/
│   │   ├── api/                # HTTP API 封装
│   │   │   └── baidu.ts        # 百度网盘 API 客户端
│   │   ├── stores/             # Pinia 状态管理
│   │   │   ├── ebook.ts        # 电子书状态
│   │   │   └── dialog.ts       # 对话框状态
│   │   ├── pages/              # 页面组件
│   │   │   ├── Home/           # 首页（书架）
│   │   │   ├── Reader/         # 阅读器页面
│   │   │   ├── FileManager/    # 文件管理器
│   │   │   ├── Settings/       # 设置页面
│   │   │   └── Callback/       # OAuth 回调页面
│   │   ├── components/         # 公共组件
│   │   ├── electron.ts         # Electron IPC API 封装
│   │   ├── wails-adapter.ts    # Wails 兼容层
│   │   ├── App.vue             # 根组件
│   │   ├── main.ts             # 应用入口
│   │   └── router.ts           # 路由配置
│   └── package.json            # 前端依赖
│
├── build/                       # 打包资源（图标等）
├── scripts/                     # 辅助脚本
│   └── check-env.js            # 环境检查脚本
│
├── package.json                 # 根项目配置
├── electron-builder.json        # Electron 打包配置
├── start.bat                    # Windows 启动脚本
├── start.sh                     # macOS/Linux 启动脚本
└── README.md                    # 本文件
```

## 🛠️ 技术栈

### 桌面框架
- **Electron 28+** - 跨平台桌面应用框架

### 前端
- **Vue 3** - 渐进式 JavaScript 框架
- **TypeScript** - 类型安全
- **Pinia** - 状态管理
- **Vue Router** - 路由管理（Hash 模式）
- **Vite** - 构建工具
- **epub.js** - EPUB 解析和渲染
- **pdf.js** - PDF 解析和渲染
- **localforage** - 本地存储（IndexedDB）

### 后端
- **Node.js** - JavaScript 运行时
- **Express** - Web 框架
- **Axios** - HTTP 客户端

## 📦 开发命令

```bash
# 安装依赖
npm install

# 开发模式（启动所有服务）
npm run dev

# 单独启动前端开发服务器
npm run dev:frontend

# 单独启动后端服务器
npm run dev:backend

# 单独启动 Electron
npm run dev:electron

# TypeScript 类型检查
cd frontend && npm run typecheck

# 构建应用
npm run build              # 构建所有平台
npm run build:win          # 仅 Windows
npm run build:mac          # 仅 macOS
npm run build:linux        # 仅 Linux
```

构建产物位于 `dist-electron/` 目录。

## 🌐 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | 3000 | Express HTTP 服务器 |
| 前端开发服务器 | 5173 | Vite 开发服务器 |
| 健康检查 | 3000/health | 后端健康检查端点 |

## 🔧 配置说明

### 百度网盘授权配置

#### 如何获取授权

1. **打开授权页面**
   - 在应用的"设置"页面，点击"获取授权"按钮
   - 或直接访问：https://openapi.baidu.com/oauth/2.0/authorize?response_type=code&client_id=hq9yQ9w9kR4YHj1kyYafLygVocobh7Sf&redirect_uri=https://alistgo.com/tool/baidu/callback&scope=basic,netdisk&qrcode=1

2. **获取授权信息**
   - 授权页面会显示以下信息：
     - **App Key (Client ID)**: 应用密钥
     - **App Secret (Client Secret)**: 应用密钥
     - **Refresh Token**: 刷新令牌
   
3. **填写授权信息**
   - 在设置页面填入上述三个信息
   - 点击"获取 access_token"按钮
   - 系统会自动验证并连接百度网盘

4. **授权成功**
   - 授权成功后会显示您的百度网盘账号信息
   - 可以开始使用云同步功能

#### 取消授权

在设置页面点击"取消授权"按钮即可断开百度网盘连接。

#### 默认配置

应用已预配置以下信息（可在授权页面获取）：
- Client ID: `hq9yQ9w9kR4YHj1kyYafLygVocobh7Sf`
- Client Secret: `YH2VpZcFJHYNnV6vLfHQXDBhcE7ZChyE`
- Redirect URI: `https://alistgo.com/tool/baidu/callback`

### 端口修改

如果端口被占用，可以修改：

**后端端口**：编辑 `backend/src/server.js`
```javascript
const PORT = 3000  // 改为其他端口
```

**前端端口**：编辑 `frontend/vite.config.ts`
```typescript
server: {
  port: 5173  // 改为其他端口
}
```

## 🧪 功能测试

### 核心功能
- [ ] 导入 EPUB 文件
- [ ] EPUB 阅读器渲染（重点）
- [ ] 导入 PDF 文件
- [ ] PDF 阅读器渲染
- [ ] 阅读进度保存和恢复
- [ ] 书籍分类管理

### 百度网盘功能
- [ ] OAuth 2.0 授权登录
- [ ] 文件列表获取
- [ ] 文件搜索
- [ ] 文件上传
- [ ] 文件下载
- [ ] 阅读进度云同步

### 系统集成
- [ ] 文件选择对话框
- [ ] 目录选择对话框
- [ ] 应用菜单
- [ ] 快捷键

## 🐛 常见问题

### 1. 端口被占用

**问题**：启动时提示端口 3000 或 5173 被占用

**解决方案**：
```bash
# Windows - 查找并结束占用端口的进程
netstat -ano | findstr :3000
taskkill /PID <进程ID> /F

# macOS/Linux
lsof -i :3000
kill -9 <进程ID>
```

或修改配置文件中的端口号。

### 2. Electron 窗口空白

**问题**：应用启动但窗口显示空白

**解决方案**：
1. 确保前端开发服务器正常运行（http://localhost:5173）
2. 按 `Ctrl+Shift+I` (Windows) 或 `Cmd+Option+I` (Mac) 打开开发者工具查看错误
3. 尝试刷新窗口：`Ctrl+R` (Windows) 或 `Cmd+R` (Mac)

### 3. 依赖安装失败（Electron 下载失败）

**问题**：npm install 报错，特别是 `RequestError: read ECONNRESET` 或 Electron 下载失败

**解决方案 1（推荐）**：使用安装脚本
```cmd
# Windows
install.bat

# macOS/Linux
chmod +x install.sh
./install.sh
```

**解决方案 2**：手动配置镜像
```bash
# 设置 Electron 镜像
npm config set electron_mirror https://npmmirror.com/mirrors/electron/

# 设置 npm 镜像
npm config set registry https://registry.npmmirror.com

# 清理并重新安装
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

**解决方案 3**：使用 cnpm
```bash
# 安装 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com

# 使用 cnpm 安装依赖
cnpm install
```

**解决方案 4**：使用代理（如果有 VPN）
```bash
npm config set proxy http://127.0.0.1:7890
npm config set https-proxy http://127.0.0.1:7890
npm install
```

### 4. EPUB 文件不显示

**问题**：EPUB 文件导入成功但内容不显示

**解决方案**：
1. 这是从 Wails 迁移到 Electron 要解决的核心问题
2. Electron 应该完美支持 epub.js 的 iframe 渲染
3. 打开开发者工具查看控制台错误信息
4. 检查 epub.js 版本和配置

## 📚 详细文档

- [快速启动指南](./QUICK_START.md) - 5 分钟快速上手
- [完整使用文档](./ELECTRON_README.md) - 详细的技术文档
- [迁移完成报告](./MIGRATION_COMPLETE.md) - Wails 到 Electron 迁移详情
- [测试检查清单](./CHECKLIST.md) - 完整的功能测试清单

## 🔄 架构说明

### 为什么从 Wails 迁移到 Electron？

**主要原因**：Wails 环境中 epub.js 的 iframe 渲染存在问题，导致 EPUB 内容无法正常显示。

**迁移收益**：
- ✅ **EPUB 渲染问题解决** - Electron 完美支持 iframe
- ✅ **技术栈统一** - 前后端都使用 JavaScript/TypeScript
- ✅ **开发体验提升** - Chrome DevTools 调试
- ✅ **生态系统成熟** - Electron 社区资源丰富

**权衡**：
- ⚠️ 打包体积增加（~50MB → ~150MB）
- ⚠️ 内存占用增加（~50MB → ~150MB）
- ⚠️ 启动速度略慢

### 兼容性设计

为了最小化前端代码修改，我们创建了 `wails-adapter.ts` 兼容层，将原有的 Wails API 调用转换为 Electron + HTTP API 调用。前端代码几乎无需修改。

## 🔐 安全性

- ✅ **Context Isolation** - 前端和主进程隔离
- ✅ **Node Integration 禁用** - 防止前端直接访问 Node.js
- ✅ **Preload 脚本** - 只暴露必要的 API
- ✅ **本地 API** - 后端只监听 localhost

## 🚧 后续计划

- [ ] 添加自动更新功能
- [ ] 优化打包体积
- [ ] 添加更多电子书格式支持
- [ ] 实现插件系统
- [ ] 性能优化和内存管理

## 📄 许可证

MIT License

## 🙏 致谢

感谢原 Wails 版本的开发工作，为本次迁移提供了坚实的基础。

---

**当前版本**：1.0.0-electron  
**迁移日期**：2026-02-03  
**状态**：✅ 基础架构完成，待测试验证

## 💡 快速提示

```bash
# 第一次使用？运行安装脚本（推荐）：
install.bat      # Windows
./install.sh     # macOS/Linux

# 或者直接安装：
npm install && npm run dev

# 遇到 Electron 下载失败？
# 1. 使用 install.bat/install.sh（已配置国内镜像）
# 2. 或手动配置镜像：
npm config set electron_mirror https://npmmirror.com/mirrors/electron/
npm config set registry https://registry.npmmirror.com

# 遇到问题？检查环境：
node scripts/check-env.js

# 需要帮助？查看文档：
# - QUICK_START.md（快速开始）
# - ELECTRON_README.md（详细文档）
# - CHECKLIST.md（测试清单）
```

**开始使用** → 运行 `install.bat` 然后 `start.bat` 🚀
